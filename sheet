#include"declaration.h"
#include"welcome.h"
#include"menu.h"
#include"newscoresheet.h"
int main()
{
int a,key;
char b;
File *fnew;
char lastinput;
while(true)
{
	
welcome;
system("cls");
menu();
ch=getch();
    char ch;
	switch(ch)
	{
	case'1';
	fileopen(ch);
	case'2'
	fileopen(ch);
	getch();
	case'3'
	dileopen(ch)
	system("cls");
	exit(0);
	}
  }
  
}
